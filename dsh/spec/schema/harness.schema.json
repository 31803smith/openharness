{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harness.autonomous.ai/dsh/spec/1/harness.schema.json",
  "title": "Domain-specific harness manifest (harness.json), spec 1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "spec",
    "id",
    "name",
    "engine"
  ],
  "properties": {
    "spec": {
      "const": 1
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}/[a-z0-9][a-z0-9-]{0,63}$",
      "description": "owner/name. The install directory under ~/.harness/dsh and the id on the wire."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 40,
      "description": "The picker tile."
    },
    "description": {
      "type": "string",
      "maxLength": 300
    },
    "category": {
      "type": "string",
      "minLength": 1,
      "maxLength": 24,
      "description": "The kind of thing it makes, in a word or two \u2014 PCB, 3D design, Slides. The picker's second line under the name."
    },
    "engine": {
      "type": "string",
      "description": "The base engine this harness runs on: claude, codex, cursor, \u2026 (one of the CLI's ENGINES)."
    },
    "workspace": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "template": {
          "type": "string",
          "description": "Directory copied into an empty workspace."
        },
        "marker": {
          "type": "string",
          "description": "Workspace-relative file whose presence means 'already materialized'."
        },
        "init": {
          "type": "string",
          "description": "Command run after the template copy, cwd = workspace."
        }
      }
    },
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "instructions": {
          "type": "string",
          "description": "File copied to <workspace>/AGENTS.md (appended under a marker if one exists)."
        },
        "skills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Directories whose SKILL.md-bearing subdirectories are linked into the workspace's skills folder."
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Set on the engine process. Values may use ${dsh}, ${workspace}, ${home}."
        },
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Appended to the base engine's argv."
        }
      }
    },
    "toolchain": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "setup": {
          "type": "string",
          "description": "Run once at install, cwd = install dir."
        },
        "doctor": {
          "type": "string",
          "description": "Exit 0 = ready. Each stdout line is shown to the user."
        }
      }
    },
    "viewer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "command",
        "url"
      ],
      "properties": {
        "command": {
          "type": "string",
          "description": "Long-running; env HARNESS_VIEWER_PORT, HARNESS_WORKSPACE, HARNESS_DSH_DIR; cwd = install dir."
        },
        "url": {
          "type": "string",
          "description": "Template with ${port} and ${artifact} (url-encoded workspace-relative path, or empty)."
        },
        "artifactExtensions": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^\\.[A-Za-z0-9]+$"
          },
          "description": "The newest file under the workspace with one of these extensions is ${artifact} when the verdict names none."
        }
      }
    },
    "verdict": {
      "type": "string",
      "description": "Workspace-relative path of the verdict file. Default .harness/verdict.json."
    }
  }
}
